#include <stdio.h>

int main(void){
    int n, s=0, a, b;
    scanf("%i", &n);
    int tab[n];
    for (int i=0; )
    while ( (c=getchar()) != EOF )
        s+= c=='a' | c=='e' | c=='i' | c=='o' | c=='u' | c=='y';
    printf("%i\n", s);
    return 0;
}
