#include <stdio.h>


#define max(a,b) (((a)>(b))?(a):(b))
int main(void){
    int n, k;
    scanf("%i", &n);
    int tab[n];
    memset(tab, 0, n);
    for (int i=0; i<n; ++i){
        scanf("%i", &k);
        if ()
    }
    printf("%i\n", ( (aa==bb+cc) || (bb==aa+cc) || (cc==aa+bb) ) + ( (a==b && a==c) << 1 ) );
    return 0;
}
